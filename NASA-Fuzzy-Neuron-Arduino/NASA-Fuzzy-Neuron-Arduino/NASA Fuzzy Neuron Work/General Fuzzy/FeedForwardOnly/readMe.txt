Based on a set of trained weights, perform the calculation. No training involved.

Weights are uploaded into specific.h

Adjust other variables as neccessary (# of inputs, number of member functions, etc).

Uses serial port to access user input. 